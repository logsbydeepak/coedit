{
  "name": "@coedit/dns",
  "private": true,
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts | pino-pretty",
    "build:clean": "rm -rf dist",
    "build:compile": "bun build --compile --minify --sourcemap src/index.ts  --outfile dist/coedit-ns",
    "build": "run-p build:clean build:compile"
  },
  "dependencies": {
    "@coedit/kv": "workspace:*",
    "@coedit/r": "workspace:*",
    "@coedit/zschema": "workspace:*",
    "@coedit/id": "workspace:*",
    "dns-packet": "^5.6.1",
    "pino": "^10.2.0",
    "pino-pretty": "^13.1.3"
  },
  "devDependencies": {
    "@types/dns-packet": "^5.6.5"
  }
}
