{
  "name": "@coedit/dns",
  "private": true,
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts | pino-pretty",
    "build:clean": "rm -rf dist",
    "build:compile": "bun build --minify --compile --sourcemap src/index.ts  --outfile dist/coedit-dns",
    "build": "run-p build:clean build:compile"
  },
  "dependencies": {
    "@types/bun": "^1.1.3",
    "@types/dns-packet": "^5.6.5",
    "dns-packet": "^5.6.1",
    "pino": "^8.20.0",
    "pino-pretty": "^11.0.0"
  },
  "devDependencies": {}
}
