{
	auto_https off
}

:80 {
	route {
		coedit_proxy stdout
		@orchestration expression {host} == "orchestration.{$ROOT_DOMAIN}"
		@server expression {host}.endsWith("-server.{$ROOT_DOMAIN}")

		reverse_proxy @orchestration localhost:5002
		reverse_proxy @server localhost:5002
	}
}
