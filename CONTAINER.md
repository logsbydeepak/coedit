# Container

```bash
# build the image
make dev
# start the container
make dev-start
# stop or remove the container
make dev-stop
```

Production container:

```bash
# build the image
make prod
```

For production container push the image to the ECR
